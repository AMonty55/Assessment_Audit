# 
A Bash Linux security assessment and hardening tool.
it checks a seerver audits it for information such as file permissions it also enables UFW firewall and creates a timestamped security score report, that the owner can only read

## Usage

```bash
chmod +x assessment_tool_lvs617.sh
./assessment_tool_lvs617.sh
```
